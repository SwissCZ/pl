#include "functions.hpp"

bool validatePrefix(std::string & formula)
{
    bool output = true;
    for (int i = 0; i < formula.length(); i++)
    {
        switch (formula[i])
        {
            
        }
    }
    return output;
}

