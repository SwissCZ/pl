#ifndef TARGET_HPP
#define	TARGET_HPP

/**
 * Program targets. Indicates the available targets of the program.
 */
enum Target
{
    DEFAULT ///< Parse input & report errors
};

#endif	/* TARGET_HPP */
